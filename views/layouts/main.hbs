<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>BookingUni</title>
    <link rel="stylesheet" href="../static/css/style.css">
</head>

<body>

    <div id="container">
        {{#if messages.error}}
        <div>
            <div class="notification loadingBox">
                Loading …
            </div>
            <div class="notification infoBox">
                <span>Info</span>
            </div>
            <div class="notification errorBox">
                <span>Error: {{messages.error}}</span>
            </div>
        </div>
        {{/if}}
        <nav>
            <div class="left-container">
                <ul>
                    <li><a href="/">Home</a></li>
                    {{#if authenticated}}
                    <li><a href="/add">Add +</a></li>
                </ul>
            </div>
            <div class="right-container">
                <a href="" class="log-out">{{name}}</a>
                <a href="/user/logout" class="log-out">Logout</a>
                {{else}}
                <a href="/user/login" class="log-out">Login</a>
                <a href="/user/register" class="log-out">Register</a>
                {{/if}}
            </div>
        </nav>

        {{{body}}}

        <footer>@SoftUni - JS Back-end - BookingUni 2020</footer>
    </div>

</body>

</html>